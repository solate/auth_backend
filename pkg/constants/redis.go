package constants

const (
	/*
		// 用户权限缓存key设计
		userPermKey := fmt.Sprintf("user:perm:%d", userId)
		rolePermKey := fmt.Sprintf("role:perm:%d", roleId)
	*/
	UserPermKey = "user:perm:%d"
	RolePermKey = "role:perm:%d"
)
